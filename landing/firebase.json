{
  "//": "hosting.ignore deliberately omits '**/.*': that glob matches the .well-known dir and would silently drop /.well-known/security.txt from the deploy (the SPA rewrite would then serve index.html as a 200 — a silent failure). 'dist' is clean Vite build output with no stray dotfiles, so the blanket dotfile ignore guards nothing. (PR #52 review.)",
  "hosting": {
    "public": "dist",
    "ignore": ["firebase.json", "**/node_modules/**"],
    "rewrites": [
      {
        "//": "Outside-in exposure teaser — same-origin so the function sees the real visitor IP and the browser never talks to Shodan directly. Must precede the SPA catch-all.",
        "source": "/api/exposure",
        "function": { "functionId": "exposureCheck", "region": "asia-south1" }
      },
      {
        "source": "**",
        "destination": "/index.html"
      }
    ],
    "headers": [
      {
        "source": "/icons/**",
        "headers": [{ "key": "Cache-Control", "value": "public, max-age=31536000, immutable" }]
      },
      {
        "source": "**/*.@(png|jpg|jpeg|svg|webp|ico)",
        "headers": [{ "key": "Cache-Control", "value": "public, max-age=604800" }]
      }
    ]
  },
  "firestore": {
    "rules": "firestore.rules",
    "indexes": "firestore.indexes.json"
  },
  "functions": [
    {
      "source": "functions",
      "codebase": "default",
      "ignore": ["node_modules", ".git", "*.log"],
      "predeploy": ["npm --prefix \"$RESOURCE_DIR\" run build"]
    }
  ],
  "emulators": {
    "auth": {
      "port": 9099
    },
    "firestore": {
      "port": 8080
    },
    "functions": {
      "port": 5001
    },
    "ui": {
      "enabled": true
    },
    "singleProjectMode": true
  }
}
