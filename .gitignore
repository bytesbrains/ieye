# OS
.DS_Store
Thumbs.db

# Editor / IDE
.idea/
.vscode/
*.swp

# Node
node_modules/
dist/
build/
*.log
.env
.env.*
!.env.example

# Flutter / Dart
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
**/ios/Pods/
**/build/

# Claude Code — internal tooling / local state, never published
.claude/
.awareness/
.supervisor/

# Scratch
tmp/

# Local-only / sensitive data — NEVER commit (this repo is PUBLIC).
# Owner-authorized security audits, network scans, device serials, credentials,
# camera playlists. Keep such material out of history entirely.
donotcommit_data/
**/donotcommit_data/
*.m3u
